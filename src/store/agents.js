export const actions = {

};

const mutations = {

};

const getters = {
  agents: state => state.agents,
};

const state = {
  agents: [],
};

export default {
  actions,
  getters,
  mutations,
  state,
};
